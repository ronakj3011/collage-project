<!DOCTYPE html>
<html>
  <head>
 <title><%= content_for(:title) || "HeartCare AI" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <!-- ✅ Main favicon -->
  <%= favicon_link_tag 'favicon1.png' %>

  <!-- ✅ Optional alternate formats -->
  <link rel="apple-touch-icon" href="<%= asset_path('favicon1.png') %>">

  <!-- ✅ (Optional) Enable PWA manifest -->
  <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <!-- ✅ Styles and JS -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  </head>

    <body>
    <!-- Header / Navbar -->
    <header class="app-header">
      <div class="header-container">
        <div class="logo-section">
        <%= link_to root_path, class: "logo" do %>
         <%= image_tag("favicon1.png", alt: "HeartCare AI", class: "logo-icon") %>
         HeartCare AI
        <% end %>

        </div>

        <nav class="nav-links">
          

          <% if user_signed_in? %>
            <%= link_to "🏠 Home", root_path, class: "nav-item" %>
              <%= link_to "🌐 About Us", about_us_predictions_path, class: "nav-item" %>

          <%= link_to "📊 Predict Risk", new_prediction_path, class: "nav-item" %>
            <%= link_to "👤 Profile", edit_user_registration_path, class: "nav-item" %>
            <%#= link_to "Logout", destroy_user_session_path	, method: :delete, data: { turbo: false } %>
            <%#= link_to "🔑 Login", new_user_session_path, class: "nav-item" %>
            <%#= link_to "📝 Sign Up", new_user_registration_path, class: "nav-item" %>
          <% end %>
        </nav>

      </div>
    </header>



    

    <!-- Main Content -->
    <main class="container">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="app-footer">
      <div class="footer-container">
        <p>© <%= Date.today.year %> HeartCare AI | Built by Group 10 using Rails & Flask ML API</p>
      </div>
    </footer>
  </body>
</html>
